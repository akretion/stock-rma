<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_rma_form" model="ir.ui.view">
        <field name="name">rma.order.form - rma_repair</field>
        <field name="model">rma.order</field>
        <field name="inherit_id" ref="rma.view_rma_form" />
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button
                    type="object"
                    name="action_view_repair_order"
                    class="oe_stat_button"
                    icon="fa-wrench"
                    groups="stock.group_stock_user"
                    invisible="type != 'customer' or repair_count == 0"
                >
                    <field
                        name="repair_count"
                        widget="statinfo"
                        string="Repair Orders"
                    />
                </button>
                <button
                    type="object"
                    name="action_view_repair_transfers"
                    class="oe_stat_button"
                    icon="fa-truck"
                    groups="stock.group_stock_user"
                    invisible="repair_transfer_count == 0"
                >
                    <field
                        name="repair_transfer_count"
                        widget="statinfo"
                        string="Repair Transfers"
                    />
                </button>
            </div>
            <xpath
                expr="//header/button[@name='%(rma.action_make_supplier_rma)d']"
                position="after"
            >
                <button
                    name="%(rma_repair.action_rma_order_line_make_repair)d"
                    string="Create Repair Order"
                    class="oe_highlight"
                    invisible="qty_to_repair == 0 or qty_to_repair &lt; 0 or state != 'approved'"
                    type="action"
                />
            </xpath>

            <xpath expr="//field[@name='rma_line_ids']/list" position="inside">
                <field name="repair_type" column_invisible="True" />
            </xpath>
            <xpath
                expr="//field[@name='rma_line_ids']/list/field[@name='product_qty']"
                position="after"
            >
                <field name="under_warranty" optional="hide" />
            </xpath>
        </field>
    </record>
</odoo>
